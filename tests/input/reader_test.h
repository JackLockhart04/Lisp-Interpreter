#pragma once

#include "util/logger.h"

#include <string>
#include <iostream>
#include <fstream>
#include <string>

class ReaderTester {
public:
    static bool runTest(int &passes, int &fails);
private:
};